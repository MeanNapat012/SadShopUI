using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

namespace InventorySystem{
    public class UICode : MonoBehaviour
    {
        /*
        [Header("Category")]
        [SerializeField] Image categoryIcon;
        [SerializaField] Text categoryText;

        [Header("Curent Item")]
        [SerializeField] Image currentItemIcon;
        [SerializeField] Text description;

        [Header("Item List")]
        [SerializeField] UIItem itemPrefab;
        [SerializeField] List<> = new ;
        // Start is called before the first frame update
        void Start()
        {
            itemUIPrefab.gameObject.setActive(false);
        }

        public void SetCategory(CategoryInfo info){
            categoryIconImage.sprite = info.icon;
            categoryText.text = info.name;
        }

        public void SetCurrentItemInfo(Itemdata data){
            descriptionText.text = data.description;
            currentItemIconImage.sprite = data.icon;
        }

        public void SetItem(UIItem_data[] uiData){

        }
        public void ClearAllItemUI(){

        }
        public void Purchase(){

        }
        // Update is called once per frame
        void Update()
        {
            
        }
        */
    }
    /*
     [Serializable]
        public class CategoryInfo{
            

        }
        */
}
